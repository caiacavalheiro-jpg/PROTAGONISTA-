let carro ={
    marca : 'fiat',
    modelo : 'fiatUno',
}

console.log(`${carro.marca} - ${carro.modelo}`)

let carroo ={
    ano : 1994,
    cor : 'vermelho',
    placa : 'xxxxx'
}

console.log (`${carroo.ano} - ${carroo.cor} - ${carroo.placa}`)